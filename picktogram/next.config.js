/** @type {import('next').nextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domain: ['picktogram-image.s3.ap-northeast-2.amazonaws.com/'],
  },
}

module.exports = nextConfig
